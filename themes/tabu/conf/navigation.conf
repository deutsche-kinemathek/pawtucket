#
# Navigation config
#

navigation = {
	"browse" = {
		"displayName" = _("Topic Search"),
		"default" = { module = , controller = Browse, action = Index },
		"requires" = {
			
		}
	},
	"advanced_search" = {
		"displayName" = _("Advanced Search"),
		"default" = { module = , controller = AdvancedSearch, action = Index },
		"requires" = {
			
		}
	}
}